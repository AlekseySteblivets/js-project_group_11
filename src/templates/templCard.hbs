{{#each this}}

<li class="main-card" id="{{id}}">
    <img class="img-main-cards" src="{{images.2.url}}" alt="name" />
    <p class="main-card-info"> {{name}}</p>
    <span class="main-card-description"> {{dates.start.localDate}} </span>
    <span class="main-card-description"> {{_embedded.venues.0.name}}</span>
</li>

{{/each}}
