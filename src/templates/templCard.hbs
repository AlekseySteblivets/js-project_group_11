{{#each this}}

<li class="main-card">
    <img class="img-main-cards" src="{{images[0]}}" alt="name" />
    <p class="main-card-info"> {{name}}</p>
    <span class="main-card-description"> {{dates.start.localDate}} </span>
    <span class="main-card-description"> {{dates.status.timezone}}</span>
</li>

{{/each}}
